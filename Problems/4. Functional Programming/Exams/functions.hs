Функции от Scheme:
    • list, append, apply, assoc, car, cdr, null?, eqv?, member, memv

Всички стандартни функции в Prelude за Haskell.

    Булеви функции:
        • not

    Числови функции:
        • fromIntegral, min, max, abs

    Функции върху списъци:
        • ++, null, length, elem, head, tail, last, sum, minimum, maximum, concat, reverse, lookup (Data.List)

    Функции от по-висок ред:
        • map, concatMap, filter, foldl, foldl1, foldr, foldr1, zip, zipWith, any, all